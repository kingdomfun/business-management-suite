{
  "name": "schedule-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "ensure-config": "node scripts/ensure-config.mjs",
    "predev": "node scripts/ensure-config.mjs",
    "dev": "vite",
    "prebuild": "node scripts/ensure-config.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "lz-string": "^1.5.0"
  }
}
